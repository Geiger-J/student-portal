package com.example.student_portal.model;

/**
 * Enum representing weekdays used in the school timetable.
 * 
 * Covers Monday through Friday for the standard school week.
 */
public enum Weekday {
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY
}