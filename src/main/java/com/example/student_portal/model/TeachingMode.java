package com.example.student_portal.model;

/**
 * Enum representing teaching mode preferences.
 * 
 * This is stored for future extension but not currently used in matching.
 */
public enum TeachingMode {
    IN_PERSON,
    ONLINE,
    HYBRID
}